<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<!-- The following infers that @users is a list of User objects; moreover, when called with a collection of users, Rails automatically iterates through them and renders each one with the _user.html.erb partial. The result iscompact code -->

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate %>
